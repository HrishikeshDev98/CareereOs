{
  "name": "careeros",
  "version": "1.0.0",
  "description": "A personal operating system to manage your entire job search journey.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "husky"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "lint-staged": {
    "client/**/*.{ts,tsx}": [
      "bash -c 'cd client && eslint --fix --max-warnings=0 \"$@\"' --",
      "bash -c 'cd client && npx prettier --write \"$@\"' --"
    ],
    "client/**/*.{json,css,md}": [
      "bash -c 'cd client && npx prettier --write \"$@\"' --"
    ]
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^17.0.7"
  }
}
